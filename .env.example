# UniOr-PET Environment Configuration

# Default Supabase Database (Free tier for all users)
# This is the shared database where user accounts and progress are stored by default
PROJECT_URL=https://your-project.supabase.co
PROJECT_API_KEY=your-supabase-anon-key
DATABASE_NAME=UniOr-PET

# Optional: Legacy MongoDB Connection (if you want to use MongoDB as default)
# MONGO_CONNECTION_STRING=mongodb+srv://username:password@cluster.domain/database

# Note: Users can still provide their own database connections during registration
# These settings are for the default/free database option
