# Core dependencies for UniOr-PET main app
streamlit==1.41.1
pandas>=1.5.3
pymongo>=4.3.3
pytz>=2023.3
supabase>=2.0.0

# Note: Heavy ML dependencies (unbabel-comet, bert-score, nltk, evaluate)
# are only needed for the management/evaluation features.
# If you need evaluation features, use requirements.full.txt instead.
